server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hw_correction?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: localhost
    port: 6379
    password: 
    database: 0

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: assign_id # 使用雪花算法生成ID

ai:
  api:
    url: "https://api.example.com/v1/chat/completions" # 替换为真实的第三方API
    key: "sk-xxxxxxxxxxxxxxxxxxxxxxxx"
